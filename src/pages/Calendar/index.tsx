import { calendarItems, weekDays } from "../../data/appData";
import { PageHeader } from "../../components/layout/PageHeader";
import { Button } from "../../components/ui/Button";
import { Card } from "../../components/ui/Card";
export function CalendarPage() { return <><PageHeader title="Your week"/><main className="page"><div className="calendar-toolbar"><div><h2>A week with room to breathe.</h2><p>October 13 – 19, 2026</p></div><div><Button variant="secondary">Today</Button> <Button variant="quiet">‹ &nbsp; ›</Button></div></div><Card className="week-card"><div className="week-grid">{weekDays.map((day,index)=><div className={`day ${index===1?"today":""}`} key={day}><b>{day}</b><div className="time-label">9 AM</div>{calendarItems.filter(item=>item.day===index).map(item=><div className={`calendar-event ${item.tone} ${item.offset?"offset":""}`} key={item.title}>{item.title}<br/><small>{item.time}</small></div>)}</div>)}</div></Card><div className="calendar-bottom"><Card><p className="eyebrow">This week</p><h3>5 commitments, 3 focus blocks</h3><p>You have a little more room Wednesday evening. It could be a gentle catch-up window.</p></Card><Card><p className="eyebrow">Protected time</p><h3>Keep Sunday open</h3><p>A quiet reset is already on your calendar. We’ll protect it.</p></Card></div></main></>; }
